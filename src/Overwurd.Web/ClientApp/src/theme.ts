import { createTheme } from '@mui/material';

export const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#6dd747',
        },
        secondary: {
            main: '#d83148',
        },
        background: {
            default: '#5c5c5c',
            paper: '#3d3d3d',
        },
    }
});