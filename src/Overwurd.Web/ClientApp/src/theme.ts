import { createTheme } from '@mui/material';

export const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#6dd747',
        },
        secondary: {
            main: '#d83148',
        },
        background: {
            default: '#0a0a0a',
            paper: '#1d2020',
        },
    }
});